<?php


function select_artifact_page_attachments(array &$page){
	$page['#attached']['library'][] = 'select_artifact/select_artifact_css';
}


function select_artifact_theme($existing,$type,$theme,$path){
	return array(
		'indexSA' => array (
			'variables' => array('artifacts' => array())
		)
	);
}